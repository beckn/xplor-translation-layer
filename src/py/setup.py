from setuptools import setup, find_packages

setup(
    name="mypackages",
    version="0.1",
    packages=find_packages(),  # Automatically find all packages in 'py'
    # Additional metadata and dependencies can be added here
)